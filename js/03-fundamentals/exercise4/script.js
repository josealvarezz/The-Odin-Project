function lastLetter(String) {
    const lastLetter = String.substring(String.length, String.length - 1);
    return lastLetter;
}

console.log(lastLetter('For this to work it should show the letter i'));